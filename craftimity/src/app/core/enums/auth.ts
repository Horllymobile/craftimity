export enum ELogin {
  EMAIL = 'email',
  PASSWORD = 'password',
}

export enum EContactType {
  EMAIL = 'email',
  PHONE = 'phone',
}

export enum EOnboardingStep {
  IMAGE_UPLOAD = 'image_upload',
  COMPLETE = 'complete',
}
